<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ilovesshan.wjhs.mapper.AccountMapper">

    <sql id="allColumns">
        `id`, `user_type`, `user_id`, `balance`, `is_delete`, `create_time`, `update_time`
    </sql>

    <insert id="insert">
        insert into
            `wjhs`.`account` (`id`, `user_type`, `user_id`, `balance`, `is_delete`)
        values
            ( #{ id },  #{ userType },  #{ userId },  #{ balance }, #{ isDelete })
    </insert>

    <update id="updateMoneyWithDecrement">
        update `wjhs`.`account` set balance = balance - #{ balance } where user_id = #{ userId }
    </update>

    <update id="updateMoneyWithIncrement">
        update `wjhs`.`account` set balance = balance + #{ balance } where user_id = #{ userId }
    </update>

    <update id="updateMoneyWithDecrementBatch">
        update
            `wjhs`.`account` set balance = balance - #{ balance }
        <where>
            user_id in
            <foreach collection="userIds" open="(" close=")" separator="," item="id" >
                #{id}
            </foreach>
        </where>
    </update>

    <update id="updateMoneyWithIncrementBatch">
        update
        `wjhs`.`account` set balance = balance + #{ balance }
        <where>
            user_id in
            <foreach collection="userIds" open="(" close=")" separator="," item="id" >
                #{id}
            </foreach>
        </where>
    </update>

    <select id="selectByUserId" resultType="com.ilovesshan.wjhs.beans.pojo.Account">
        select <include refid="allColumns" /> from  `wjhs`.`account` where  user_id = #{ userId } and is_delete = '15'
    </select>
</mapper>